# Загружаем базовый образ Node.js с использованием Alpine Linux
FROM node:alpine
# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /time-app
# Копируем файлы package.json и если есть package-lock.json из текущей директории внутрь контейнера в рабочую директорию установленную выше
COPY package*.json ./
# Выполняем команду npm install внутри контейнера, чтобы установить зависимости
RUN npm install
# Копируем все файлы и папки из текущей директории внутрь контейнера
COPY . .
# Открываем порт 8954 в контейнере, чтобы внешние приложения могли обращаться к приложению, запущенному внутри контейнера, используя этот порт
EXPOSE 8954
# Задаем команду, которая будет выполняться при запуске контейнера
CMD ["npm", "run", "dev"]
